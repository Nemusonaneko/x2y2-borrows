{
  "name": "x2y2-borrows",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.thegraph.com/deploy/ nemusonaneko/x2y2-borrows",
    "create-local": "graph create --node http://localhost:8020/ nemusonaneko/x2y2-borrows",
    "remove-local": "graph remove --node http://localhost:8020/ nemusonaneko/x2y2-borrows",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 nemusonaneko/x2y2-borrows",
    "test": "graph test"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.36.1",
    "@graphprotocol/graph-ts": "0.28.1"
  },
  "devDependencies": { "matchstick-as": "0.5.0" }
}
